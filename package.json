{
  "name": "perler-miniapp-mvp",
  "version": "0.2.0",
  "private": true,
  "description": "图片转拼豆图纸微信小程序 MVP",
  "scripts": {
    "build:palette": "node scripts/build-palette.js",
    "test": "node --test tests/*.test.js",
    "check": "node scripts/validate-data.js && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
